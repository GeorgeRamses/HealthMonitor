<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Network ───────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- ── Camera ────────────────────────────────────────────────────────── -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- ── Notifications ─────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!--
        USE_FULL_SCREEN_INTENT — required on Android 12+ (API 31+) for the
        notification's setFullScreenIntent() to actually promote to a full-screen
        alarm UI. Without this the notification stays in the shade as a heads-up
        only, and the alarm activity is never launched from the notification path.
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <!-- ── Alarms ─────────────────────────────────────────────────────────── -->
    <!--
        SCHEDULE_EXACT_ALARM — required on Android 12+ to schedule exact alarms
        via AlarmManager.setExactAndAllowWhileIdle / setAlarmClock.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!--
        USE_EXACT_ALARM — Android 13+ alternative that does NOT require a runtime
        permission dialog (granted automatically for alarm/clock apps).
        Declaring both lets the system pick the right one per API level.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- ── Boot ───────────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- ── Wake lock (keeps CPU alive long enough to play alarm sound) ────── -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- ── Vibration ─────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!--
        SYSTEM_ALERT_WINDOW — required to draw overlay windows (pop-ups) while
        running in the background. This is essential for Xiaomi and other OEM devices
        to display full-screen alarm notifications. The app needs this permission
        to ensure alarms display properly on restricted OEM devices.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
            android:name=".HealthMonitorApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            tools:targetApi="31">

        <!-- ── Main launcher activity ──────────────────────────────────────── -->
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
            ── MedicationAlarmActivity ─────────────────────────────────────────
            This activity is launched by MedicationAlarmReceiver when a dose is due.
            It must be declared here for Android to accept startActivity() calls from
            a BroadcastReceiver context, and for the lock-screen / screen-on attributes
            to take effect at install time.

            Key attributes explained:
              android:showOnLockScreen="true"
                  Tells the WindowManager this activity may appear over the lock screen.
                  Works in tandem with setShowWhenLocked(true) called in onCreate().

              android:turnScreenOn="true"
                  Asks the PowerManager to wake the display when this activity starts.
                  Works in tandem with setTurnScreenOn(true) called in onCreate().

              android:launchMode="singleInstance"
                  Gives the alarm its own back-stack task. Without this, launching the
                  alarm while the main app is in the foreground re-uses the existing task
                  and the window flags are ignored because the window is already visible.

              android:excludeFromRecents="true"
                  Prevents the alarm screen from appearing in the recents list after it
                  is dismissed — keeps the task switcher clean.

              android:directBootAware="true"
                  Allows the activity to start before the user has unlocked the device
                  after a reboot (required for alarms scheduled via Direct Boot).
        -->
        <activity
                android:name=".util.MedicationAlarmActivity"
                android:exported="false"
                android:showOnLockScreen="true"
                android:turnScreenOn="true"
                android:launchMode="singleInstance"
                android:excludeFromRecents="true"
                android:directBootAware="true"/>

        <!-- ── Alarm broadcast receiver ────────────────────────────────────── -->
        <!--
            android:directBootAware="true" — allows the receiver to fire before the
            user has unlocked the device after a reboot, so alarms scheduled during
            Direct Boot are delivered correctly.
        -->
        <receiver
                android:name=".util.MedicationAlarmReceiver"
                android:exported="true"
                android:directBootAware="true"/>

        <!-- ── Boot receiver (reschedules alarms after reboot) ─────────────── -->
        <receiver
                android:name=".util.BootReceiver"
                android:exported="true"
                android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!--
            ── FileProvider ────────────────────────────────────────────────────
            Required for sharing exported report files via ACTION_SEND.
            The authority must match exactly what ExportViewModel passes to
            FileProvider.getUriForFile(): "${packageName}.provider"
        -->
        <provider
                android:name="androidx.core.content.FileProvider"
                android:authorities="${applicationId}.provider"
                android:exported="false"
                android:grantUriPermissions="true">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_provider_paths"/>
        </provider>

    </application>

</manifest>