<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Network ───────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- ── Camera ────────────────────────────────────────────────────────── -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- ── Notifications ─────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!--
        USE_FULL_SCREEN_INTENT — required on Android 12+ (API 31+) for the
        notification's setFullScreenIntent() to actually promote to a full-screen
        alarm UI. Without this the notification stays in the shade as a heads-up
        only, and the alarm activity is never launched from the notification path.

        On Android 14+ (API 34+) this permission must ALSO be granted by the user
        at runtime via Settings → Special app access → Alarms & reminders.
        The app checks canUseFullScreenIntent() and prompts accordingly.
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" tools:ignore="FullScreenIntentPolicy"/>

    <!-- ── Alarms ─────────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:ignore="ExactAlarmPolicy"/>

    <!-- ── Boot ───────────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- ── Wake lock ──────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- ── Vibration ─────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- SYSTEM_ALERT_WINDOW removed — setShowWhenLocked() handles lock screen
         display without this permission, which triggers Play Store review flags -->

    <application
            android:name=".HealthMonitorApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            tools:targetApi="31">

        <!--
            Disable automatic Crashlytics data collection until the user
            explicitly consents via the consent dialog on first launch.
            Collection is enabled programmatically in HealthMonitorApplication
            only after consent is granted.
        -->
        <meta-data
                android:name="firebase_crashlytics_collection_enabled"
                android:value="false" />

        <!--
            Disable WorkManager's default initializer so we can provide our own
            Hilt-aware factory via Configuration.Provider in HealthMonitorApplication.
        -->
        <provider
                android:name="androidx.startup.InitializationProvider"
                android:authorities="${applicationId}.androidx-startup"
                android:exported="false"
                tools:node="merge">
            <meta-data
                    android:name="androidx.work.WorkManagerInitializer"
                    android:value="androidx.startup"
                    tools:node="remove"/>
        </provider>

        <!-- ── Main launcher activity ──────────────────────────────────────── -->
        <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
            ── MedicationAlarmActivity ─────────────────────────────────────────

            android:showWhenLocked="true"
                API 27+ attribute. Renders this window over a SECURE lock screen
                (PIN/pattern/fingerprint) without requiring authentication.
                Works in tandem with setShowWhenLocked(true) in onCreate().
                Both the manifest attribute AND the programmatic call are needed
                for full OEM coverage (MIUI/ColorOS only respect the manifest).

            android:showOnLockScreen="true"
                Older OEM alias for showWhenLocked. Kept for Xiaomi/OPPO compat.

            android:turnScreenOn="true"
                API 27+ attribute. Wakes the display when this activity starts.
                Works in tandem with setTurnScreenOn(true) in onCreate().

            android:launchMode="singleInstance"
                Gives the alarm its own isolated task. Without this, the alarm
                launched while the main app is open re-uses the existing task
                and window flags are silently ignored.

            android:excludeFromRecents="true"
                Keeps the task switcher clean after the alarm is dismissed.

            android:directBootAware="true"
                Allows the activity to start before first unlock after reboot.

            android:screenOrientation="portrait"
                Prevents rotation from restarting the sound/vibration cycle.
        -->
        <activity
                android:name=".util.MedicationAlarmActivity"
                android:exported="false"
                android:showWhenLocked="true"
                android:showOnLockScreen="true"
                android:turnScreenOn="true"
                android:launchMode="singleInstance"
                android:excludeFromRecents="true"
                android:directBootAware="true"
                android:screenOrientation="portrait"/>

        <!-- ── Alarm broadcast receiver ────────────────────────────────────── -->
        <receiver
                android:name=".util.MedicationAlarmReceiver"
                android:exported="true"
                android:directBootAware="true"/>

        <!-- ── Notification tap receiver ───────────────────────────────────── -->
        <!--
            Handles taps on medication alarm notifications.
            Cancels the notification first, then opens MainActivity.
            This is what makes the notification disappear when the user taps it —
            Android does not auto-cancel ongoing notifications via contentIntent.
            Not exported — only used internally via PendingIntent.getBroadcast().
        -->
        <receiver
                android:name=".util.NotificationTapReceiver"
                android:exported="false"/>

        <!-- ── Missed dose follow-up receiver ──────────────────────────────── -->
        <receiver
                android:name=".util.MissedDoseReceiver"
                android:exported="false"
                android:directBootAware="true"/>

        <!-- ── Boot receiver ───────────────────────────────────────────────── -->
        <receiver
                android:name=".util.BootReceiver"
                android:exported="true"
                android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- ── FileProvider ────────────────────────────────────────────────── -->
        <provider
                android:name="androidx.core.content.FileProvider"
                android:authorities="${applicationId}.provider"
                android:exported="false"
                android:grantUriPermissions="true">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_provider_paths"/>
        </provider>

    </application>

</manifest>