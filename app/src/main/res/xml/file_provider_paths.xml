<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
        cache-path covers getCacheDir() — this is where ExportViewModel writes
        the temporary .txt report before sharing it via the Android share sheet.
    -->
    <cache-path name="exported_reports" path="." />
</paths>